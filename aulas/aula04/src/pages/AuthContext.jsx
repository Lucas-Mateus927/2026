import {creatContext} from
const [usuario, setUsuario] = useState({});
const [logado, set logado] = useSate(false);

const login = (dados) => {
    //chama a API backend
    setUsuario({nome: "Jose"});
    setLogado(true);
}